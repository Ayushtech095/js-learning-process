console.log("ayush ghosh")

 const name = "ayush"
    const sirname = "ghosh"

    //console.log(name + sirname)
   console.log(`my first name is ${name} and my last name is ${sirname}`)
   const h= "harrypotter"
   console.log(h.substring(0,4))
   //similarly we have slice for the same task but slice as supports negative intergers which can allow reverse indexing

   const n = "   brown fox      "
   // trim function removes unwanted spces
   console.log(n.trim())
   console.log(n)
